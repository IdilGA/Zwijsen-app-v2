# Azure Storage connection string (keep this private, only used server-side)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account;AccountKey=your_key;EndpointSuffix=core.windows.net

# Azure Blob Storage container name (publicly accessible in Nuxt client if needed)
AZURE_STORAGE_CONTAINER_NAME=scans
